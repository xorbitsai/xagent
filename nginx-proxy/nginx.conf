# 主配置：由 start.sh 生成 xagent-proxy.generated.conf 后生效
worker_processes 1;
error_log logs/nginx-proxy-error.log;
pid        nginx-proxy.pid;

events {
    worker_connections 1024;
}

http {
    default_type  application/octet-stream;
    include       xagent-proxy.generated.conf;
}
